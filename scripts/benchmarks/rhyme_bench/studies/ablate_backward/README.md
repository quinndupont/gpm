# Ablate — backward construction

See [CARD.yaml](CARD.yaml).

Tests whether explicit **backward** rhyme construction instructions (lock end-words from the closing positions first, then draft lines) change strict rhyme metrics versus the default forward framing.
